{
  "config": {
    "step": {
      "user": {
        "title": "MeteoSwiss Radar",
        "description": "Adds the MeteoSwiss radar proxy and the `meteoswiss-radar-card` Lovelace card. No settings required.\n\nAfterwards, reload the browser tab (in the Companion app: reset the frontend cache) — otherwise dashboards report \"Custom element doesn't exist: meteoswiss-radar-card\"."
      }
    },
    "abort": {
      "single_instance_allowed": "Already configured. Only a single instance is allowed."
    }
  }
}
